#!/bin/bash
# This additional setup is required even if installing veil through apt
# Veil currently fails setups, so disbaling for now
#chmod +x /usr/share/veil/config/setup.sh
#/usr/share/veil/config/./setup.sh --force --silent
